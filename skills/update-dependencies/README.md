# Update Dependencies Skill

Use to systematically update dependencies while minimizing risk of build failures, broken APIs, and runtime regressions. Core principle: **verify before writing, triage before fixing, understand before removing.**
